[project]
name = "datasight-llm-server"
version = "0.1.0"
description = "CPU-safe FastAPI gateway for DataSight LLM requests on DSRI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0,<1.0.0",
    "httpx>=0.27.0,<1.0.0",
    "pydantic-settings>=2.6.0,<3.0.0",
    "uvicorn[standard]>=0.30.0,<1.0.0",
]

[tool.uv]
package = false
