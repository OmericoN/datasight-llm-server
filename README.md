# DataSight LLM Server

Minimal vLLM server for running a Qwen model on DSRI.

## What it does

This container starts a vLLM OpenAI-compatible API server on port `8000`.

Default model:

```text
Qwen/Qwen2.5-0.5B-Instruct
